remote_theme: pages-themes/slate@v0.2.0
plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have one
title: "EXT4 on macOS using Docker"
description: "A guide to mounting and working inside ext4 partitions on macOS using Docker for Mac."
